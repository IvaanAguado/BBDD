--5.4
--a
SELECT
	MAX(LIBRO.PRECIO)
FROM
	LIBRO;

--b
SELECT
	MIN(LIBRO.PRECIO)
FROM
	LIBRO;

--c
SELECT 
	MIN(FECHA_ENT) 
FROM 
	EDITA
WHERE
	EDITA.DNI = 443042293;

--d
SELECT
	MAX(FECHA_ENT)
FROM
	LIBRO
WHERE
	EDITA.DNI= 29110302;

--e
SELECT 
	MAX(NUMPAG) 
FROM 
	LIBRO 
WHERE 
	TEMATICA = 'Novela';

	
--f
SELECT 
	MIN(NUMPAG) 
FROM 
	LIBRO 
WHERE 
	LIBRO.TEMATICA IN ('Infantil', 'Científica', 'Novela', 'Cómico');

--g
SELECT
	COUNT(*)
FROM
	LIBRO
WHERE
	LIBRO.TEMATICA = 'Científico' 
AND
	LIBRO.PRECIO BETWEEN 5 AND 15;
